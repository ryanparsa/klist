id: CLST-37
stage: active
title: Protect Static Pod Manifests Directory
description: |
  Static pods are managed directly by kubelet on each node by reading YAML manifests from a
  local directory (typically `/etc/kubernetes/manifests/`). Attackers who gain write access
  to this directory can create or modify pods that run outside the Kubernetes API server's
  control - bypassing admission controllers, RBAC, and audit logging.

  Kubelet also supports a `--manifest-url` flag that loads manifests from a remote URL.
  Deploying static pods from web-accessible locations can be risky. Both vectors must be locked down.

  **What to check:**
  - Verify `/etc/kubernetes/manifests/` is only writable by root:
    ```
    stat -c "%a %U" /etc/kubernetes/manifests
    ```
  - Verify kubelet is not started with `--manifest-url`:
    ```
    ps aux | grep kubelet | grep manifest-url
    ```
mitigations:
  - |-
    Set directory permissions to `700` owned by `root`. Avoid using web-hosted manifests for Kubelet.
    Ensure Kubelet does not run with the `--manifest-url` argument unless strictly required.
    Monitor the directory for unexpected file changes using a file integrity monitoring (FIM) tool.
tags:
  - node-security
  - hardening
  - persistence
  - kubelet
tools:
  - title: kube-bench
    url: https://github.com/aquasecurity/kube-bench
  - title: Falco
    url: https://falco.org/
references:
  - title: Kubernetes Docs - Static Pods
    url: https://kubernetes.io/docs/tasks/configure-pod-container/static-pod/
  - title: Microsoft Threat Matrix - Avoid using web-hosted manifest for Kubelet
    url: https://microsoft.github.io/Threat-Matrix-for-Kubernetes/mitigations/MS-M9032%20Avoid%20using%20web-hosted%20manifest%20for%20Kubelet/
  - title: Microsoft Threat Matrix - Static Pods
    url: https://microsoft.github.io/Threat-Matrix-for-Kubernetes/techniques/Static%20Pods/
  - title: CIS Kubernetes Benchmark 4.1.9
    url: https://workbench.cisecurity.org/benchmarks/16828
