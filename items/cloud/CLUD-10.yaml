id: CLUD-10
title: Do Not Mount the Docker Socket into Containers
stage: active
description: |
  Mounting the Docker daemon socket (`/var/run/docker.sock`) into a container grants that
  container full control over the Docker daemon - effectively root on the host node. This is
  a common container-escape vector exploited in CI/CD pipelines and build systems.

  **What to check:** Search all workloads for volume mounts referencing `/var/run/docker.sock`.

  ```
  kubectl get pods -A -o json | \
    jq '.items[].spec.volumes[]? | select(.hostPath.path == "/var/run/docker.sock")'
  ```
mitigations:
  - |-
    Replace Docker-in-Docker approaches with rootless, socket-less alternatives
    such as Kaniko, Buildah, or img for image builds. Use OPA/Kyverno admission policies to
    block hostPath mounts of the Docker socket.
tags:
  - node-security
  - privilege-escalation
  - container-runtime
tools:
  - title: Kaniko
    url: https://github.com/GoogleContainerTools/kaniko
  - title: Buildah
    url: https://buildah.io/
  - title: Kyverno
    url: https://kyverno.io/
  - title: Trivy
    url: https://trivy.dev/
references:
  - title: CIS Docker Benchmark v1.5
    url: https://www.cisecurity.org/benchmark/docker
  - title: "OWASP Kubernetes Top 10 - K01: Insecure Workload Configurations"
    url: https://owasp.org/www-project-kubernetes-top-ten/
  - title: Kubernetes Docs - Volumes / hostPath
    url: https://kubernetes.io/docs/concepts/storage/volumes/#hostpath
