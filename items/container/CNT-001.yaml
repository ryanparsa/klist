id: CNT-001
title: Enforce Pod Security Standards at Namespace Level
description: |
  Pod Security Standards (PSS) define three profiles - Privileged, Baseline, and Restricted -
  that control which security-sensitive fields pods may set. Namespaces must be labelled to
  enforce at least the Baseline profile; production namespaces should target Restricted.

  **What to check:**

  ```
  kubectl get namespaces -o json | \
    jq '.items[].metadata.labels | with_entries(select(.key | startswith("pod-security.kubernetes.io")))'
  ```

  Every non-system namespace should have `pod-security.kubernetes.io/enforce` set to
  `baseline` or `restricted`.
mitigations:
  - |-
    Add PSS labels to all namespaces. Use `warn` and `audit` modes first to
    detect violations before switching to `enforce`. Consider OPA Gatekeeper or Kyverno for
    more fine-grained policies.
tags:
  - pod-security
  - admission-control
  - hardening
tools: []
references:
  - title: Kubernetes Docs - Pod Security Standards
    url: https://kubernetes.io/docs/concepts/security/pod-security-standards/
  - title: NSA/CISA Kubernetes Hardening Guide
    url: https://media.defense.gov/2022/Aug/29/2003066362/-1/-1/0/CTR_KUBERNETES_HARDENING_GUIDANCE_1.2_20220829.PDF
  - title: PCI-DSS Requirement 2 - Apply Secure Configurations
    url: https://www.pcisecuritystandards.org/document_library/
  - title: Microsoft Threat Matrix - Privileged Container
    url: https://microsoft.github.io/Threat-Matrix-for-Kubernetes/techniques/Privileged%20container/
