id: CONT-6
title: Apply a Seccomp Profile to All Containers
stage: active
description: |
  Seccomp (Secure Computing Mode) filters the system calls a container can make to the
  Linux kernel. Without a seccomp profile, a compromised container can invoke dangerous
  syscalls (e.g., `ptrace`, `mount`, `keyctl`) that may lead to container escape.

  **What to check:**

  ```
  kubectl get pods -A -o json | \
    jq '.items[] | select(
      .spec.securityContext.seccompProfile.type == null and
      .spec.containers[].securityContext.seccompProfile.type == null
    ) | .metadata.name'
  ```
mitigations:
  - |-
    Set `seccompProfile.type: RuntimeDefault` (or `Localhost` with a custom
    profile) at the pod `securityContext` level. The RuntimeDefault profile is safe for
    virtually all workloads and is required by PSS Restricted.

    ```yaml
    securityContext:
      seccompProfile:
        type: RuntimeDefault
    ```
tags:
  - security
  - seccomp
  - hardening
  - linux
tools: []
references:
  - title: CIS Kubernetes Benchmark 5.7.2
    url: https://workbench.cisecurity.org/benchmarks/16828
  - title: Kubernetes Docs - Seccomp
    url: https://kubernetes.io/docs/tutorials/security/seccomp/
  - title: NSA/CISA Kubernetes Hardening Guide
    url: https://media.defense.gov/2022/Aug/29/2003066362/-1/-1/0/CTR_KUBERNETES_HARDENING_GUIDANCE_1.2_20220829.PDF
